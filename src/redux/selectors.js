export const selectedUsers = (state) => state.users.users;
export const selectedFollow = (state) => state.isFollow.follows;
export const selectedError = (state) => state.users.error;
export const selectedLoader = (state) => state.users.isLoading;
export const selectedFilter = (state) => state.filter.filter;
